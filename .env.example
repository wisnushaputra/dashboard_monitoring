# =============================================================================
# NOC Dashboard — Docker Compose Environment Variables
# =============================================================================
# Copy this file to .env and adjust values for your environment:
#   cp .env.example .env
# =============================================================================

# Database
DB_PASSWORD=password
DB_PORT=5433

# Backend
JWT_SECRET=noc-dashboard-secret-change-in-production

# Ports (change if conflicts on host)
BACKEND_PORT=4000
FRONTEND_PORT=3000
